# FreeBSD

准备在VMWare安装FreeBSD系统。官方 FreeBSD 磁盘镜像（通常称为 Base System）默认只包含纯命令行环境（CLI）。这是 FreeBSD 的设计哲学：它提供一个最精简的底座，用户需要什么功能（包括桌面环境）就自己安装什么。

后续TODO(期末考完)：安装桌面环境（如 XFCE）。参考：https://gemini.google.com/app/eff4a9944ae08d78